{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build-frontend": "cd ../frontend && npm run build && cp -r dist/* ../backend/public/",
    "dev": "npm run build-frontend && npm start",
    "update-frontend": "cd ../Frontend && npm run build && cp -r dist/* ../Backend/public/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
